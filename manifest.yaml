id: serverboards.secrets
name: Secret manager
description: |
  Store encrypted blobs of information on Serverboards.

  It uses symetric encryption and is all performed in the user interface, so no
  plain data is stored in the backend ever.

components:
  - id: screen
    type: screen
    name: Secrets
